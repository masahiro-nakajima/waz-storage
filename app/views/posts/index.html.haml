%h1 Posts
= link_to 'New', new_post_path

= notice if notice.present?

- @posts.each do |post|
  .post{style: 'border: solid 1px'}
    .title
      = post.title
      = link_to 'Detail', post_path(post)
      = link_to 'Edit', edit_post_path(post)
      = link_to 'Delete', post_path(post), :method => :delete
    .body
      = post.body
